# コントリビューション & Git運用ガイドライン

本プロジェクトにおけるGitブランチ運用およびプルリクエスト（PR）のルールです。

---

## 1. ブランチ戦略

- **`main` ブランチ**
  - 公開（本番）環境です。
  - **`main` への直接コミット・直接プッシュは禁止**されています。
- **作業ブランチ**
  - 要件や設計方針が確定し、実装を開始するタイミングで最新の `main` からブランチを作成します。
  ```bash
  git checkout main
  git pull origin main
  git checkout -b <プレフィックス>/<作業概要>
  ```

### ブランチ命名規則
- フォーマット: `<type>/<kebab-case-description>`
- プレフィックス:
  - `feature/`: 新機能・機能追加
  - `fix/`: バグ修正
  - `refactor/`: リファクタリング
  - `chore/`: ビルド・設定・環境整備・雑務
  - `docs/`: ドキュメント関連

---

## 2. コミット規約

- コミットメッセージは**日本語**で記述します。
- 変更内容が分かりやすいようにPrefixをつけて記述することを推奨します（例: `feat: ...`, `fix: ...`, `chore: ...`）。

---

## 3. プルリクエスト（PR）フロー

1. 作業完了後、作業ブランチをリモートへプッシュします。
2. GitHub CLI (`gh`) またはGitHub Web画面から `main` 宛てにPRを作成します。
3. PRタイトルおよび本文は**日本語**で記述し、以下の構成を含めます：
   - **概要**: 目的や背景
   - **変更点**: 具体的な変更リスト
   - **確認内容**: 実施した動作確認やテスト内容
